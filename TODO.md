# TODO — что не реализовано в MVP (locrest-client)

1. **Vendored chisel отменён** — клиент теперь использует внешний `github.com/jpillora/chisel`. Если в будущем потребуется форк или vendor — вернуть в `internal/chiselvendor/`.
